# Changelog

تمام تغییرات مهم این پروژه در این فایل ثبت می‌شود.

## [1.1.0] - 2026-07-25

### افزوده شد
- **ماژول مشترک `shared.js`**: تمام منطق تکراری بین `popup.js`، `content.js` و `options.js` در یک فایل مشترک متمرکز شد (فراخوانی API، مدیریت تاریخچه، تشخیص جهت متن، پرامپت‌های لحن، نگاشت زبان‌ها).
- **میانبر کیبورد**: با `Ctrl + Enter` (یا `Cmd + Enter` در مک) می‌توانید مستقیماً از داخل کادر متن ترجمه را شروع کنید.
- **پشتیبانی از زبان‌های بیشتر**: ایتالیایی، روسی، ترکی، چینی، ژاپنی، کره‌ای، هندی و پرتغالی به فهرست زبان‌ها اضافه شدند (مجموعاً ۱۴+ زبان).
- **حذف تکی آیتم‌های تاریخچه**: حالا می‌توانید هر رکورد تاریخچه را جداگانه حذف کنید، بدون نیاز به پاک کردن کل تاریخچه.
- **ترجمه‌ی مجدد خودکار**: با تغییر زبان مقصد یا لحن در ویجت شناور، ترجمه به‌صورت خودکار به‌روزرسانی می‌شود.

### اصلاح شد
- **ناسازگاری ساختار تاریخچه**: `popup.js` رکوردها را با کلیدهای `targetL` و `time` ذخیره می‌کرد، در حالی که `content.js` از `targetLang` و `timestamp` استفاده می‌کرد. این باعث می‌شد بخشی از تاریخچه به‌درستی نمایش داده نشود. اکنون هر دو از یک ساختار واحد استفاده می‌کنند.
- **مدیریت خطا**: پیام‌های خطای واضح‌تر، به‌خصوص وقتی کلید API تنظیم نشده است (`MISSING_API_KEY`).
- **امنیت**: کلید API هنگام قرار گرفتن در URL به‌درستی encode می‌شود.
- **پارس کردن SSE**: پردازش استریم به‌صورت بافرشده انجام می‌شود تا چانک‌های ناقص باعث خطا نشوند.
- **escape کردن HTML**: کاراکترهای نقل‌قول نیز escape می‌شوند تا از تزریق جلوگیری شود.

### تغییر کرد
- ساختار پروژه بازسازی شد و حجم کد تکراری به‌طور چشمگیری کاهش یافت.
- مستندات `README.md` به‌روزرسانی شد.

## [1.0.0]

- انتشار اولیه: ترجمه با Gemini، ویجت شناور، منوی راست‌کلیک، پاپ‌آپ، صفحه تنظیمات، تبدیل متن به گفتار و تاریخچه.
